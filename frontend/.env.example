# Base URL for the FastAPI backend.
# In development, the backend runs on port 8000 by default.
# In production/staging, point this to your deployed backend origin.
NEXT_PUBLIC_API_URL=http://localhost:8000
